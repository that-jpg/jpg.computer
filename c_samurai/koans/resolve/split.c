#include <stdio.h>

char **split(char string[], int size, char delimiter) {
  return;
}

int main() {
  char string[] = "Joio trigo";
  int string_size = 256;
  char **strings;
  strings = split(string, string_size, ' ');
  printf("Vitoria? Vitoria!");
}
